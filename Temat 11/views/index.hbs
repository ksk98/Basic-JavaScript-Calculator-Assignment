<div class="container" style="background-color: whitesmoke">
    <h1>{{ title }}</h1>

    <div class="row">
        {{# each items }}
            <div class="col-sm">
                <img src="{{ this.sciezka }}/>{{ this.nazwa }}.png" alt="..." width="200">
                <figcaption class="figure-caption text-left">
                    nazwa: {{ this.nazwa }} rozmiar: {{ this.rozmiar }}kb
                </figcaption>
            </div>
        {{/each}}
    </div>
    <br><br>
    <div style="text-align: center;">
        <form action="pictures/create" method="get">
            <button class="brn btn-primary" type="submit" class="btn-link">Dodaj zdjęcie</button>
        </form>
    </div>
</div>